# Copy to .env.local and fill in values

# Required for live mode — leave blank to use demo mode
ANTHROPIC_API_KEY=

# Demo mode (default: true)
# Set to false to use real Anthropic API stream
DEMO_MODE=true

# Exposed to client — must match DEMO_MODE
NEXT_PUBLIC_DEMO_MODE=true
